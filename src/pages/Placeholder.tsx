export default function Placeholder({ title }: { title: string }) {
  return (
    <div className="placeholder">
      <h1>{title}</h1>
      <p>This section is a placeholder in the Wise frontend demo.</p>
    </div>
  )
}
